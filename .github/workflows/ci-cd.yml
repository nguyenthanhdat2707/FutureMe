name: CI/CD Pipeline

on:
  push:
    branches: [main, develop, 'feat/**']
  pull_request:
    branches: [main, develop]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: nguyenthanhdat2707/futureme

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm install --no-audit --no-fund

      - name: Run ESLint
        run: npm run lint

  test:
    name: Unit Tests
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm install --no-audit --no-fund

      - name: Run tests
        run: npm test

      - name: Upload coverage
        uses: codecov/codecov-action@v3
        with:
          files: ./coverage/lcov.info
          flags: unittests
          name: codecov-umbrella
        continue-on-error: true

  build:
    name: Build & Scan
    runs-on: ubuntu-latest
    needs: test
    permissions:
      contents: read
      packages: write
      security-events: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=ref,event=pr
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=sha,prefix={{branch}}-
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: false
          load: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
          format: 'sarif'
          output: 'trivy-results.sarif'
          severity: 'CRITICAL,HIGH'

      - name: Upload Trivy results to GitHub Security
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: 'trivy-results.sarif'
        continue-on-error: true

      - name: Trivy scan report
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
          format: 'table'
          exit-code: '1'
          ignore-unfixed: true
          severity: 'CRITICAL,HIGH'

      - name: Push Docker image
        if: github.event_name == 'push'
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

#   deploy-staging:
#     name: Deploy to Staging
#     runs-on: ubuntu-latest
#     needs: build
#     if: github.ref == 'refs/heads/develop' || startsWith(github.ref, 'refs/heads/feat/')
#     environment:
#       name: staging
#       url: https://staging.future-me.example.com
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4
# 
#       - name: Deploy to staging
#         run: |
#           echo "Deploying to staging environment..."
#           echo "Image: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}"
#           # Add your deployment commands here:
#           # Example for Kubernetes:
#           # kubectl set image deployment/future-me-backend future-me-backend=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
#           # Example for Docker Compose on remote server:
#           # ssh user@staging-server "cd /app && docker-compose pull && docker-compose up -d"
#           # Example for AWS ECS:
#           # aws ecs update-service --cluster staging --service future-me --force-new-deployment
# 
#       - name: Wait for deployment
#         run: sleep 30
# 
#   smoke-test:
#     name: Smoke Tests
#     runs-on: ubuntu-latest
#     needs: deploy-staging
#     if: github.ref == 'refs/heads/develop' || startsWith(github.ref, 'refs/heads/feat/')
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4
# 
#       - name: Health check
#         run: |
#           # Replace with your actual staging URL
#           STAGING_URL="${STAGING_URL:-https://staging.future-me.example.com}"
#           
#           echo "Running smoke tests against: $STAGING_URL"
#           
#           # Health check
#           response=$(curl -s -o /dev/null -w "%{http_code}" "$STAGING_URL/api/health" || echo "000")
#           
#           if [ "$response" = "200" ]; then
#             echo "✓ Health check passed"
#           else
#             echo "✗ Health check failed (HTTP $response)"
#             exit 1
#           fi
# 
#       - name: API endpoint tests
#         run: |
#           STAGING_URL="${STAGING_URL:-https://staging.future-me.example.com}"
#           
#           # Test context endpoint
#           response=$(curl -s -o /dev/null -w "%{http_code}" "$STAGING_URL/api/context" || echo "000")
#           if [ "$response" = "200" ]; then
#             echo "✓ Context API responding"
#           else
#             echo "✗ Context API failed (HTTP $response)"
#             exit 1
#           fi
# 
#       - name: Notify deployment success
#         if: success()
#         run: |
#           echo "🚀 Deployment to staging successful!"
#           echo "Environment: staging"
#           echo "Commit: ${{ github.sha }}"
#           echo "Branch: ${{ github.ref_name }}"
# 
#   deploy-production:
#     name: Deploy to Production
#     runs-on: ubuntu-latest
#     needs: build
#     if: github.ref == 'refs/heads/main'
#     environment:
#       name: production
#       url: https://future-me.example.com
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4
# 
#       - name: Deploy to production
#         run: |
#           echo "Deploying to production environment..."
#           echo "Image: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}"
#           # Add your production deployment commands here
# 
#       - name: Wait for deployment
#         run: sleep 30
# 
#       - name: Production smoke test
#         run: |
#           PROD_URL="${PROD_URL:-https://future-me.example.com}"
#           response=$(curl -s -o /dev/null -w "%{http_code}" "$PROD_URL/api/health" || echo "000")
#           
#           if [ "$response" = "200" ]; then
#             echo "✓ Production health check passed"
#           else
#             echo "✗ Production health check failed"
#             exit 1
#           fi
# 
#       - name: Notify production deployment
#         if: success()
#         run: |
#           echo "🎉 Production deployment successful!"
#           echo "Commit: ${{ github.sha }}"
